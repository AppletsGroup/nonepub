import { Meta } from '@storybook/react'
import DragHandle from './index'

export default {
  component: DragHandle,
  title: 'Components/DragHandle',
} as Meta

export const Default = () => <DragHandle />
